# Request

## Schema

```json
{
    "app": "string",
    "method": "string",
    "params": "object",
    "v": "number"
}
```
All these properties are required.
`params` object is being validated by a schema of specified method.
