{
    "watch": [
        "src/**/*.ts"
    ],
    "ignore": [
        "src/**/*.spec.ts"
    ],
    "ext": "ts,json,env",
    "exec": "npm run fix-lint && npm run lint && npm run build && npm run start"
}
